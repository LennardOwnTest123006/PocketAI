<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Only the export folder is ever shared, and only through an explicit
         share sheet action started by the user. -->
    <cache-path name="exports" path="exports/" />
</paths>
