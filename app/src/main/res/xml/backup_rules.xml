<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Conversations and settings may be backed up; multi-gigabyte model files
         must never be, so they are excluded explicitly. -->
    <exclude domain="file" path="models/" />
</full-backup-content>
